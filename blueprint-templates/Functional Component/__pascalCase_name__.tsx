import React from "react";
import { View } from "react-native";

import { getStyles } from "./styles";

interface IProps {

}

export const {{pascalCase name}}: React.FC<IProps> = ({ }) => {

  const s = getStyles({});

  return (
    <View>

    </View>
  );
};
