import { COLORS } from './colors';
import { SPACE } from './space';

export const borderRadius = {
  small: 5,
  medium: 15,
  large: 30,
};

export const lightShadow = {
  elevation: 3,
  shadowColor: COLORS.grey150,
  shadowOffset: {
    height: 2,
    width: 1,
  },
  shadowRadius: 5,
  shadowOpacity: 0.2,
};

export const ELEMENTS = {
  card: {
    padding: 100,
  },
};
